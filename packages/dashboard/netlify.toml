[build]
  command = "ROOT=$(git rev-parse --show-toplevel 2>/dev/null || pwd); cd \"$ROOT\" && corepack enable && pnpm install --frozen-lockfile && pnpm --filter @clientpad/dashboard build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
