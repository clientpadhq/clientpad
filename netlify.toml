[build]
  command = "npm install -g pnpm@9.12.0 && pnpm install --frozen-lockfile && pnpm --filter @clientpad/dashboard build"
  publish = "packages/dashboard/dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
